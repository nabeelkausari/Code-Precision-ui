import React from 'react';
import { MainRoutes } from "../main"
import '../../styles/abstracts/_light-colors.scss'

export default (props) => {
  console.log('Light Theme Loaded')
  return (
    <MainRoutes/>
  );
};
