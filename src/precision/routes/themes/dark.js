import React from 'react';
import { MainRoutes } from "../main"
import '../../styles/abstracts/_dark-colors.scss'

export default (props) => {
  console.log('Dark Theme Loaded')
  return (
    <MainRoutes/>
  );
};
