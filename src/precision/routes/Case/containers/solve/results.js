import { connect } from 'react-redux';
import {getResultsError} from "../../../../modules/case/actions"

function mapStateToProps(state, ownProps) {
    return {

    }
}

export default connect( mapStateToProps, {getResultsError})